# Automatically generated by espanso migration tool
# Original file: user/datetime.yml

matches:
  - trigger: $$
    replace: "{{mydate}}"
    vars:
      - name: mydate
        type: date
        params:
          format: "%Y-%m-%d"
  - trigger: "@@"
    replace: $$
  - trigger: "%%"
    replace: "{{mydate}}"
    vars:
      - name: mydate
        type: date
        params:
          format: "%H:%M"